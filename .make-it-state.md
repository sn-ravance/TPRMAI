# Project State -- AI TPRM Machine (TPRMAI)
> Last updated: 2026-03-17
> Last session: retrofit-it (Phase A complete)

## Current Status

**Retrofit in progress -- Phase A complete, Phase B next.**

The app is running in Docker with PostgreSQL and mock-oidc. Phase A (development environment) is verified and working.

## Retrofit Overview

**Risk Score: 42 (High) -- Phased retrofit mode**

| Phase | Description | Status |
|-------|-------------|--------|
| A | Development environment (Docker, PostgreSQL, mock-oidc) | COMPLETE |
| B | Secure login and user permissions (OIDC, RBAC, JWT) | PENDING |
| C | AI Provider (multi-provider abstraction, model tiering) | PENDING |
| D | UI + Integration (standard components, DataTable, admin pages) | PENDING |
| E | Security + Infra (seed data alignment, Terraform, .ship-it.yml) | PENDING |

## Phase A -- What Was Done

| File | Action |
|------|--------|
| `prisma/schema.prisma` | Changed SQLite to PostgreSQL, added Alpine binary targets |
| `Dockerfile` | Created multi-stage build with OpenSSL for Prisma |
| `docker-compose.yml` | Created: app (3020), db (5438), mock-oidc (10091 dev profile) |
| `entrypoint.sh` | DB wait, schema push, seed, then start Next.js |
| `next.config.js` | Added `output: 'standalone'` for Docker |
| `.env.example` | Updated: PostgreSQL, OIDC placeholders, AI provider config |
| `.env` | Created for local development |
| `.dockerignore` | Created to optimize build context |
| `.gitignore` | Unignored prisma/migrations and /scripts/ |
| `mock-services/mock-oidc/` | Copied from DeliverIt (complete mock OIDC provider) |
| `scripts/seed-mock-services.sh` | User registration, cleanup, URI update |

## Running Services

```
docker compose --profile dev up -d
```

| Service | Container | Port (host) | Port (internal) |
|---------|-----------|-------------|-----------------|
| App | tprmai-app | 3020 | 3000 |
| PostgreSQL | tprmai-db | 5438 | 5432 |
| mock-oidc | tprmai-mock-oidc | 10091 | 10090 |

## mock-oidc Test Users

| Subject | Email | Role (target) |
|---------|-------|---------------|
| admin-001 | admin@tprmai.local | ADMIN |
| analyst-001 | analyst@tprmai.local | ANALYST |
| viewer-001 | viewer@tprmai.local | VIEWER |
| vendor-001 | vendor@tprmai.local | VENDOR |

## Tech Stack

- Next.js 16.1.6 (monolith with API routes)
- Prisma ORM with PostgreSQL (was SQLite)
- 6 AI Agents: VERA, CARA, DORA, SARA, RITA, MARS
- Claude via Azure AI Foundry (PRIMARY AI provider)
- Docker Compose with mock-oidc
- Radix UI + Tailwind CSS

## Phase B Plan (Next)

Replace NextAuth credentials with OIDC, add database-driven RBAC:

1. Add RBAC tables (roles, permissions, role_permissions) via Prisma migration
2. Migrate string `role` column to role_id FK
3. Replace NextAuth with OIDC auth flow (login, callback, JWT cookie, /me, logout)
4. Add requirePermission middleware on all API routes
5. Wire sidebar to show/hide based on user permissions
6. Add admin UI: User Management + Role Management pages

## Known Issues

1. Login page still has "Continue to Demo" button (remove in Phase B)
2. Uses `next/font/google` Inter font (Zscaler may block -- fix in Phase D)
3. No permission checks on API routes (add in Phase B)
4. Plain HTML tables on vendors/findings pages (replace with DataTable in Phase D)
5. AI agents use LangChain AzureChatOpenAI (migrate to multi-provider in Phase C)

## Previous History

- 2026-03-16: Imported from OneDrive, security fixes, AI provider clarification
- 2026-03-17: Retrofit Phase A -- Docker, PostgreSQL, mock-oidc

# =============================================================================
# TPRMAI Environment Configuration
# =============================================================================
# Copy to .env for local development: cp .env.example .env

# ---------- Database ----------
# Docker Compose (local): postgresql://tprm_user:tprm_pass@localhost:5438/tprm_db
# Docker internal:        postgresql://tprm_user:tprm_pass@db:5432/tprm_db
DATABASE_URL="postgresql://tprm_user:tprm_pass@localhost:5438/tprm_db"

# ---------- Authentication (NextAuth - current, OIDC coming in Phase B) ----------
NEXTAUTH_SECRET=  # Generate with: openssl rand -hex 32
NEXTAUTH_URL="http://localhost:3020"

# OIDC (Phase B -- uncomment after OIDC retrofit)
# OIDC_ISSUER_URL=http://localhost:10091        # Mock OIDC for local dev
# OIDC_CLIENT_ID=mock-oidc-client
# OIDC_CLIENT_SECRET=mock-oidc-secret
# JWT_SECRET=                                    # Generate with: openssl rand -hex 32

# ---------- AI Provider ----------
# Supported: anthropic_foundry, claude, openai, ollama, docker
AI_PROVIDER="anthropic_foundry"

# Azure AI Foundry with Claude (Entra ID auth -- no API key needed)
# Prerequisites: az login, access to Azure AI Foundry resource
AZURE_OPENAI_ENDPOINT="https://your-resource.openai.azure.com"
AZURE_OPENAI_API_VERSION="2024-08-01-preview"

# Model assignments per agent complexity tier
AI_MODEL_COMPLEX="claude-opus-4-6"          # CARA, SARA (deep analysis)
AI_MODEL_STANDARD="claude-sonnet-4-5"       # VERA, RITA, MARS (standard reasoning)
AI_MODEL_SIMPLE="claude-haiku-4-5"          # DORA (document retrieval)

# Fallback deployment (optional)
AZURE_OPENAI_FALLBACK_DEPLOYMENT_NAME="claude-opus-4-5"

# Direct Claude API (alternative to Azure AI Foundry)
# ANTHROPIC_API_KEY=

# OpenAI API (alternative)
# OPENAI_API_KEY=

# Ollama (local, alternative)
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3

# ---------- External Security APIs (optional) ----------
SECURITY_SCORECARD_API_KEY=""
BITSIGHT_API_KEY=""
DNB_API_KEY=""

# ---------- File Upload ----------
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE="52428800"
